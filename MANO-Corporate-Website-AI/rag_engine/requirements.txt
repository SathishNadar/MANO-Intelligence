# Python dependencies for offline data pipeline scripts only
# (chunk_data.ipynb and index_knowledge.py)
# The live server now runs in Node.js — see package.json

chromadb>=1.4.0
sentence-transformers>=3.0.0
python-dotenv>=1.0.0
